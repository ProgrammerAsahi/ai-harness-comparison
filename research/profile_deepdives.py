"""Mechanism-level explanations and failure analysis inferred from cited designs."""
DEEP = {
'C01': '''记忆至少有准确性、相关性和时效三个问题。保存的内容可能原本就错，也可能正确但与当前任务无关，还可能已经过期。长期使用应定期抽查重要事实与偏好，保留来源和修订方式。一次成功找回旧对话，只验证了检索的一部分。

Skill 应保存稳定的方法，例如核对报告数字的步骤；项目进度则记录特定任务现在做到哪里。把临时失败、未经确认的偏好或推测都写入长期方法，会让错误在以后反复出现。更合理的做法是完成任务并核对后，再提炼可复用经验。

子代理提供独立上下文，减少主任务被探索过程挤满，但也需要完整交接。研究者应返回来源、冲突和不确定项，而不只交一个结论。预算应合计主任务、所有委派、记忆处理与失败重试。消息渠道和定时任务启用后，还要检查身份、服务在线和重复动作，这些属于持续助理的完整成本。''',
'C02': '''多个渠道进入同一个个人代理，会同时提出身份与顺序问题。必须知道输入来自谁、应该进入哪个会话、能调用哪些工具；同一任务的补充要求也应与原任务正确关联。配对成功只是连接起点，后续权限与结果归属仍需验证。

串行化可以减少同一会话的争写，但跨会话、后台任务和外部系统还可能共享资源。旧任务被取消后是否仍有资格保存状态，关系到新结果会不会被覆盖。试用时故意修改目标或取消任务，检查实际文件和记忆，比只测试一问一答更能说明可靠性。

长期服务需要备份和恢复，而记忆内容也要能修订。把服务搬到另一台机器，除了聊天记录，还可能涉及工作空间、凭据、设备连接和工具环境。迁移时逐项验证；即使选用相同模型，运行位置和状态缺失也会使行为不同。''',
'C03': '''通用计算环境可以通过临时脚本处理复杂资料，这是它与纯聊天的重要差别。脚本产生数字时，应保留输入、筛选条件、计算方法和失败文件清单；模型最后的解释需要依据这些结果。能运行程序带来可验证性，也要求用户真正利用这些证据。

扩展在多个循环节点介入，会让定制更灵活，也增加行为来源。某次修改提示或工具后质量下降，应从最小配置重现，逐项恢复；同时更换模型和多个扩展，会使原因难以判断。子代理继承哪些背景、使用哪份配置，也应写进记录。

容器隔开部分运行环境，实际影响范围仍取决于挂载与授权。把全部个人目录和广泛业务凭据放进去，会改变原本期待的边界。先给任务专用输入与输出目录，完成后检查产物，再逐步增加能力，更容易理解每项工具到底贡献了什么。''',
'C04': '''聊天、文件讨论和工作模式的验收不同。聊天主要看内容，资料任务要看引用是否支持结论，Work 还要看文件是否正确保存、工具有没有完成操作。选择最轻而足够的模式，可以减少普通写作被复杂执行流程打断。

上下文快照使会话拥有较稳定的起点。修改全局偏好后旧任务仍沿用原语气，可能是快照生效范围问题；重新确认当前上下文比不断追加同一句要求更直接。项目事实也不应全放进人格，因为其他项目未必适用，而且事实可能过期。

多模型比较时，将事实准确与文风偏好分开。先用原文核对遗漏、数字和否定，再评价自然程度；两个模型都说了同一件事，也不能替代来源。需要长期写小说时，人物与事件应落在可编辑文件中；需要工作报告时，保留材料和口径比持续增长的聊天历史更好查阅。''',
'C05': '''桌面宿主管理会话生命周期，驱动管理具体执行，这有助于统一入口，却也形成两层状态。界面保留了任务记录，后端可能已经退出或改变配置；恢复时要确认驱动、模型、目录和工具仍然匹配。只看聊天记录还在，不能判断工作现场已完整恢复。

多模型同时回答适合比较观点与文风，但事实判断需要原始证据。若模型读取同一份错误摘要，它们的共识没有独立验证价值。真正的分工应有不同职责与交付件，例如研究给来源表、写作给正文、最后核对两者，而不是多开几个相似助手。

后端切换时也应分别处理权限与费用。Claude、Pi、DSH 的工具、认证和恢复行为并非由桌面壳统一消除。对普通用户，先稳定一条常用路线更省事；只有明确需要另一种执行方式时再增加驱动，避免把管理所有后端变成新的日常工作。''',
'C06': '''知识问答失败要分层检查：原文有没有正确解析，关键片段是否检索到，后端是否允许该用户读取，模型有没有忠实理解。每层都可能造成相似的错误答案。用带标准来源的问题逐项核对，能防止盲目提高生成模型成本。

多人门户增加了共享与隔离的要求。共享模型端点不代表共享知识权限，共享工具也不代表应共用创建者凭据。接终端后还要考虑文件、进程和资源是否按用户分开；单纯网页登录只解决身份的一部分。

离线部署应检查全部依赖路径。生成模型在内网，但嵌入、搜索或语音走云端，仍会形成不同的数据出口。备份也不应只保存网页配置，还需覆盖历史、资料和必要的服务状态。自托管提供控制机会，实际控制程度由部署与管理决定。''',
'C07': '''Agent 配置本身是一种共享资产，既包含职责，也可能关联强工具。谁能修改它、谁能使用它、工具以谁的身份调用，应分别明确。团队试点可以用两个权限不同的用户验证同一任务，观察资料和工具范围是否按预期变化。

服务端运行跨越模型请求、工具执行和持久化。模型已经返回结果而数据库保存失败，与模型本身失败不同；工具执行成功但客户端断连，也不应直接触发重复操作。排错应查看实际运行与外部状态，再决定继续或重试。

子代理和代码执行可以提高任务能力，同时增加追踪与资源需求。研究 Agent 的结论应带来源，计算 Agent 的输出应带方法与输入，最终负责人再核对。大量角色若没有清楚交接，只会把一份错误分散到更多对话中。自托管团队应先稳定少数高频职责，再逐步扩展。''',
'C08': '''运行时将“决定动作”与“执行动作”分开，便于增加工具、审批和结束处理。用户看到任务完成时，仍应能找到对应文件或结果；状态标签不能脱离真实产物。这种有状态设计的收益，主要在任务可追踪和可接续。

从同一基础状态并行执行工具，适合多个独立查询；如果两项动作都要更新同一对象，结果合并需要业务规则。例如两个角色同时修订同一段报告，应先确定归属或交由统一编辑者整合。底层可以收齐结果，不代表语义冲突自动解决。

角色设计可以从最小职责开始：研究负责事实，写作负责表达，核验负责回原文。若只是改变语气，一份提示模板可能已经足够。只有需要不同状态、工具或权限时才增加独立 Agent，这样既减少调用成本，也让读者知道每项结论由谁依据什么产生。''',
'C09': '''资料上传只是第一步。扫描文件可能提取失败，表格可能失去列关系，同一内容的多个旧版本也可能同时进入索引。模型检索到片段后仍可能缺少上下文。抽查导入内容与引用位置，往往比反复调整生成提示更能解决问题。

嵌入服务与生成模型承担不同职责。更换嵌入方案后，应核对现有索引是否仍适用；更换生成模型，则主要影响理解、表达和工具行为。两项一起改变会让对照失去解释性，最好先稳定检索，再比较答案质量。

Agent 与 Flow 的差别在于下一步由谁决定。步骤固定的提取和归档，显式 Flow 更容易检查变量和失败位置；需要临时调查的问题可交给 Agent。无论哪条路线，都应保留输入、输出与来源。多人使用还要将工作空间当作权限与资料边界来设计，而不仅是方便分类的文件夹。''',
'D01': '''流程图适合表达可确定的业务顺序，但模型仍会引入不确定输出。每个节点应有明确输入与输出约定，必要时增加结构检查；上游字段缺失时，下游不能默默以常识补齐。调试从节点数据开始，通常比直接修改整条提示更有效。

Agent 节点应放在真正需要探索的环节。若只是固定分类或提取，让模型自由调用许多工具可能增加成本与失败路径；若资料明显不足，流程应返回缺口或人工处理，而不是一直要求更长推理。外层流程和局部自治各用在适合的位置，才能发挥组合价值。

应用发布后，模型、知识源和提示的变化都可能改变结果。保留代表性样本与可工作的流程版本，变更后重点回归受到影响的分支。多人业务应用还要区分终端使用者与流程管理者的权限，不能因为作者能访问某项资料，就把它暴露给所有应用用户。''',
'D02': '''自动化的核心问题之一是重复。网络超时可能发生在动作已成功之后，因此重试前需要业务标识或状态检查。让模型承诺不重复，无法替代确定的去重逻辑。创建记录、发送消息和更新状态尤其需要清楚的恢复设计。

字段与批量数据也容易出错。上游返回空数组、多个对象或不同日期格式时，下游节点可能收到与预期不同的输入。先检查节点数据和转换，再判断模型是否理解错误；把所有结构问题包装成自然语言交给模型，会降低流程可解释性。

Agent 节点适合处理需要理解的局部工作，固定业务顺序由工作流管理。逐步从手动触发到定时运行，可以把内容质量、流程正确性与长期可靠性分开验收。成本除了模型，还包括连接器配额、运行资源和处理失败的人工时间，长期任务尤其需要这本总账。''',
'D03': '''短循环使实验变量更容易控制，但并不意味着环境可以忽略。相同模型和提示，在不同依赖、工具或测试版本中也可能得到不同结果。保留固定仓库状态与执行环境，是解释成功率的基础。

轨迹分析应找到第一个实质性偏差：没找到关键文件、生成错误动作、误解工具输出，还是在预算结束前没有验证。这样可以提出具体改进，而不是笼统说模型不够聪明。最后文本中的自评只是一条消息，独立检查才决定任务是否完成。

对照实验还要防止只挑成功案例。记录全部任务、终止原因、重试和预算；模型输出随机时观察多次结果，工具配置变化则一次只改一项。它适合把代理机制讲清楚，但研究结果推广到日常产品时，仍需考虑界面、权限、历史和人工协作这些额外层次。''',
'D04': '''工具接口不仅是功能集合，也影响模型怎样思考和行动。文件展示方式、错误提示和编辑格式都会改变后续选择；复现实验若更换这些条件，即使模型相同，也不再是原来的组合。研究旧结果时应把工具配置与提示当作实验的一部分保存。

格式重试能帮助纠正不合法动作，却不能证明合法动作有意义。不断执行同样的错误调查仍可能消耗预算，必须回到轨迹看模型是否利用了反馈。把格式失败、环境失败和推理失败分开，才能判断改进应该落在哪里。

维护转向 mini 后，旧项目仍可用于理解历史方法，但新工作应评估维护责任。迁移时先保留任务与验收，逐步替换执行器和工具，而不是一次改变全部条件。无法取得原模型或环境时，应明确称为新的对照研究，保留与历史实验的差异。''',
'D05': '''编排层能够复用经验，也可能把不适合的流程强加给简单任务。每个新增角色都需要输入、输出、等待和汇总，存在固定成本。只有子任务足够独立且规模适当时，并行收益才可能超过协调开销。

依赖关系应在实施前表达。后端接口还没确定时让前端自主实现，往往产生返工；两个工作者同时修改共享配置，也可能互相覆盖。计划文件不仅列任务，还应记录接口、责任和停止条件，主代理保留集成判断。

多层升级是常见排错难点。底层 Harness 改工具格式，模型服务改行为，插件又改角色默认值，最终结果可能同时变化。一次只更新一层，保留最小任务和不使用编排的基线，能帮助判断是组合问题还是模型问题。可读的证据材料也让退出或迁移更容易。''',
'B01': '''编辑器天然拥有即时线索，但这些线索不一定完整。当前文件很容易进入上下文，真正的错误却可能在配置或后端；索引能帮助查找，又可能漏掉排除目录。让代理说明它依据哪些文件作判断，可以发现“读到了很多内容”与“找到了关键证据”的区别。

本地协作适合边做边改需求：用户看到 diff 或预览后立即反馈，代理再修正。云端委托则要求任务说明更加完整，因为当前编辑器里的未提交改动、打开文件和本机服务不会自动成为相同环境。选择后台任务前，应先把隐含背景写进仓库或任务。

Auto 和固定模型解决不同需求。Auto 省配置，但路由变化会影响费用和复现；固定型号便于比较，却要求自己判断什么时候升级。界面、检索、工具与模型共同影响结果，不能用一次 Auto 任务得出的感觉永久评价某个模型。最可靠的比较是同一仓库状态、同类任务和相同验收。''',
'B02': '''团队协作对象本身就是上下文。issue 说明为什么做，分支保存改动，PR 连接审查与 CI。把这些材料写清楚，有助于云端代理独立工作；如果需求只存在于口头沟通，代理就可能根据常见做法补出错误假设。任务发起者需要把关键例外写成明确条件。

IDE 侧公开实现与托管服务是不同证据范围。前者能帮助研究编辑器怎样组织上下文和工具，后者的环境、调度与内部服务只能依据公开文档判断。旧仓库迁移也只是开发位置变化，不能单独证明品牌停止或全部核心已公开。

PR 中的测试结果要回到需求看覆盖范围。构建通过只能证明某类静态或运行检查成功，不能证明新权限和业务边界都正确。代理批量产生 PR 后，审查可能成为瓶颈；选型时应统计每项变更需要多少人工核验，而不是只统计一天创建了多少任务。''',
'B03': '''远程委托首先是把项目变成可独立启动的工作环境。依赖、测试数据、内部服务和权限缺一项，代理都可能停在准备阶段。将环境问题与需求问题分开记录，能够判断是否应该改善安装脚本、补充业务背景，还是换更强模型。

项目知识与索引帮助找到线索，但对关键接口仍需回到最新代码。自动生成的说明可能省去新人探索时间，也可能保存过时结构；迁移任务尤其应要求列出真实调用方和兼容依据。任务跨度越长，越需要阶段成果和明确停点，避免一次性大改后才发现方向错误。

组织自管 Outposts 一类环境，会把部分平台依赖转成自己的基础设施责任。网络、凭据和数据范围更可控，同时也需要容量、升级与故障恢复。比较托管与自管不应只看月费，还要计入环境准备和运维的人力；这与本地 Desktop 的即时使用是不同成本结构。''',
'B04': '''规范可以减少歧义，也可能把模型猜测固化成看似正式的文档。审核时先找无法由已有材料确定的业务决定，如取消条件、权限与默认值，把它们交给真正了解业务的人确认。用规范约束实施的前提，是规范本身值得信任。

需求、设计和任务应保持对应关系。某项需求没有实施任务，容易被遗漏；某项任务找不到需求依据，可能是无关扩展。完成后将实际实现和设计对照，记录合理偏差，才能让这些材料继续用于维护，而不是交付后立即过时。

Hooks 适合在确定时机执行检查，Skills 适合复用需理解的方法，MCP 适合连接外部能力。这些扩展不能替代需求判断。简单修复可以直接完成；只有协调和返工成本值得时，再使用完整 Spec 流程。评价 Kiro 时，应把规范节约的沟通和维护规范的时间一起计算。''',
'B05': '''IDE 语义能力只有在项目正确加载时才有价值。错误 SDK、缺失模块或未解析依赖会使导航与诊断不完整，代理可能误判引用关系。开始试用前人工跑通基础检查，能够把环境缺陷与代理能力分开。

大型重构应先比较符号关系，再做小批修改。纯字符串替换可能误伤同名变量，语义导航则有助于区分不同对象；但代理是否真正采用正确工具仍要看实际结果。要求它列出受影响调用方和兼容条件，比笼统说“使用 IDE 能力”更容易验收。

规划与实施用不同模型时，交接材料必须可独立理解。架构理由、接口、异常条件与不可改变行为都应进入实施任务，不能只留一句“按计划做”。如果更便宜的实施模型因上下文不足反复返工，分工并未真正节约。模型组合应根据最终可审查补丁与总时间决定。''',
'B06': '''ACP 将交互与执行分开，但双方仍需正确协商能力。客户端能展示的命令、附件或审批，与代理实际支持的内容可能不同。不能因为在同一面板看到多个后端，就推定每项按钮在它们之间语义相同；应按具体功能试用。

遇到问题时可以先在外部代理自身入口重现。若独立运行也失败，检查代理认证、模型和工具；独立运行正常而编辑器中失败，再检查启动命令、目录与协议连接。这种分层排错，比同时重装编辑器和更换模型更有效。

统一编辑器能保留文件浏览、快捷键和审阅习惯，降低替换代理的成本；历史、规则和权限仍可能绑定后端。切换前把关键进度落成可读材料，再由新代理核对真实文件。它降低的是一部分绑定，而不是让整套执行状态随时无损搬家。''',
'B07': '''成品任务往往有多个质量门槛。资料报告需要事实、引用、结构与排版；应用原型需要运行、流程、数据与权限。产品将这些步骤放在同一界面，并不意味着每一步都会自动达标。使用者应为每阶段提出可观察结果，避免只在最后检查一个看起来完整的文件。

闭源产品的角色名、模式名和宣传图不足以证明内部调度。报告只能依据公开功能解释工作分工；实际选型应观察它怎样处理缺材料、工具失败和需求变化。若每次失败都只能整体重做，长期成本可能高于首次生成时的便利。

不同地区、桌面和 Web 入口可能使用不同模型或工具，比较时需要完整记录。对于代码项目，保存运行和部署说明；对于资料项目，保存原文与引用。这样既方便审查，也减少以后更换工作台时对私有会话的依赖。商业产品和研究仓库应始终分开评价。''',
'B08': '''上下文服务的错误可以分成三类：材料没进入索引，进入后没被检索到，检索到了却被代理理解错。三类问题对应范围、检索和模型推理，不能全靠升级大模型解决。用已知答案提问，是低成本区分这些问题的方法。

跨仓库检索尤其需要时效和权限。找到某个旧接口的全部调用方，并不意味着找齐新版本的调用方；可访问一个仓库，也不能推定可访问整个组织。让答案保留代码位置与版本线索，有助于判断它是否与当前修改对象一致。

Context Engine 与外部 Harness 配合时，要分别评价找材料和执行任务。可以先让原代理在没有检索服务时调查，再加入服务比较探索时间与遗漏；实施阶段保持相同模型。若同时换模型、索引和代理，虽然体验可能改善，却很难判断该继续投入哪一层。''',
'B09': '''能力档位是一组产品决策。升档后不仅可能思考更多，还可能调用不同专家或采用不同工具。因此，比较档位是在比较完整配置，而不是孤立测模型智力。记录实际调用与结果，才能解释更高档究竟改善了哪一步。

独立上下文的专家适合处理边界清楚的问题。向 Oracle 提问“这段并发控制的两个假设哪个更符合日志”，通常比让它泛泛审查整个项目更容易得到可用结论。交回的依据必须让主代理能继续核验，否则专家只是又一层听起来权威的回答。

历史访问和持久远程环境解决不同连续性问题：前者找回曾经讨论的材料，后者保存工作现场的某些条件。两者都需要核对现状，不能把旧线程说服务正常当作当前环境正常。小任务不必使用全部能力，减少无效专家调用有时比单纯换便宜模型更有帮助。''',
'B10': '''复杂任务最容易在交接处丢信息。规划者知道整体目标，工作者只看到分配给自己的部分；如果共享接口、错误行为和测试要求没写清，各自完成后可能无法组合。Mission 的任务材料应围绕这些交接点设计，而不只是把大目标拆成很多标题。

非交互工作者不能随时问人，使“输入是否充分”成为重要条件。可以先让主任务解决业务疑问，再把稳定部分交出去；尚未确定接口的前后端不宜同时自主设计。并行的收益应来自真正独立的工作，而非同时修改同一个高耦合模块。

验证应看实际产物和环境，不只看工作者总结。一个独立检查角色若仍只阅读成功声明，并没有形成独立验收。模型成本应合计规划、所有工作者、验证和返工；当集成成为主要负担时，减少代理或缩小批次往往比继续增加并行更有效。''',
'B11': '''终端背景有助于定位问题，但输入范围仍需选择。一个失败命令后可能产生很多连锁报错，代理应找到首个相关原因，而不是逐条机械修复。保留原命令、目录、退出状态和关键输出，可以帮助区分环境错误与代码错误。

将临时操作保存成 Profile，只是固定了一组能力与设置；把它变成自动化还需定义输入、触发、失败和结果保存。一次救火中临时扩大过的权限，不应不加判断地成为日常任务默认。对于会修改外部状态的任务，重跑前还要检查上一次是否已经生效。

Warp 中运行第三方 CLI 时，代理模型和会话仍由对方管理。原生 Agent、第三方代理和远程平台可能同时出现在同一个终端体验中，但账单和权限不一定合并。排错时先确定实际执行者与主机，再查看对应配置，可以避免在错误产品里寻找模型设置。''',
'B12': '''浏览器证据需要和验证问题对应。首页截图只支持外观判断，不能证明表单校验、网络错误或权限正确。要求代理写出具体操作与观察结果，可以把“测试过了”变成可检查证据，也方便用户复现。

视觉任务包含两类迭代：一类是布局与样式，另一类是交互与状态。前者可通过不同尺寸截图发现问题，后者需要点击、输入和等待真实响应。只改截图里看得见的元素，容易遗漏焦点、加载、错误与长文本等状态。

代码、终端和浏览器分别拥有自己的环境与权限。页面能打开，但后端可能仍使用测试数据；工具能点击，但登录身份未必是目标用户。验收时明确测试环境和角色，再判断结果能否推广到真实使用。闭源内部调度不可见，反而更应把可见产物与操作证据组织清楚。''',
'B13': '''平台降低了开发环境门槛，也容易让原型与生产系统看起来过于相似。几条测试数据可以展示流程，真实使用却要考虑并发、输入错误、权限与数据恢复。应在引入真实用户前明确哪些部分已经验证，哪些只是演示。

部署费用与 Agent 费用应分开。一次生成完成后，应用可能继续占用计算、数据库或外部服务；为了上线不断返工，也会继续产生开发消费。先估算一个月实际使用规模，再选择部署方案，比只比较首轮生成价格更合理。

迁移演练可以发现隐藏依赖。把代码取出，在独立环境按说明启动，检查数据库、环境变量与部署配置是否齐全；只下载源码不能证明可离开平台。对初学者而言，托管起步的便利仍很有价值，但应把数据与运行方式尽早记录，而不是等业务扩大才第一次梳理。''',
'B14': '''Repo Wiki 为陌生项目建立入口，但它仍是根据代码与资料形成的说明。最需要核对的是关键业务规则、数据流和跨模块关系，而不是目录名是否都出现。说明与源码冲突时，先查真实行为与版本，不让生成文档成为新的错误权威。

目标式执行需要当前目标、进度和验证条件共同存在。切换模型前只保存初始需求，可能丢失已经排除的方案和失败原因。把这些材料写成可读检查点，再由新会话核对实际文件，可以减少重复调查。

主模型与辅助知识能力的收费范围不同，是多入口产品常见的取舍。用户使用自有 Key 时，仍应检查平台是否对 Wiki、远程任务或其他服务另外扣费。比较时不仅要记录调用量，也要观察这些能力是否减少了理解项目的人工时间；只有费用和收益一起看，才知道整合是否值得。''',
'B15': '''上下文引用可以减少代理猜测，但要选择正确证据。报错来自运行时，Git diff 描述最近改动，接口文档说明预期契约，三者各有作用。代理需要把它们联系起来，而不是只因为某个文件最近改过，就认定它一定是根因。

子代理的职责与权限应一起定义。“研究员”可以要求只返回结论，却仍可能继承命令工具；真正只读需要对应执行限制。团队或工作树功能则另涉及目录、取消与结果集成，不能由一个角色配置文件推断全部能力。

相关生态可以减少查找资料和配置入口的成本，但实际项目仍可能使用不同 API 版本或自定义组件。让代理对照当前依赖与真实输出验证。国内外版本、插件和 CLI 的模型及额度也应分别记录，避免把一个入口的成功经验直接当作整个产品家族的承诺。''',
'B16': '''持续目标需要明确结束条件。比如“完成配置迁移”应包含哪些调用方必须替换、哪些兼容测试必须通过，而不是只凭模型觉得工作差不多。目标越抽象，自动继续越可能变成无止境扩展；设置阶段成果与验收有助于让任务及时停下。

暂停恢复是检查状态质量的好方法。让代理指出已完成文件、实际验证和下一步，再与磁盘核对；若只复述最初目标，说明恢复材料不够。人手修改过文件后尤其要重新核对，避免旧检查点覆盖最新进展。

项目指令迁移也需区分一次导入与持续读取。修改旧规则文件后行为没有变化，可能是当前运行时已经使用另一份文件，而非模型故意不遵守。先核对真实来源与生效范围，再调整提示或推理档位。原生模型适配能改善部分执行，不能替代任务状态和项目配置正确性。''',
'B17': '''插件在编辑器里获得选区和缓冲区内容，但用户仍需确认任务涉及哪些文件。只选中一个函数时，模型可能不了解调用方约定；盲目扩大上下文又会增加干扰。先局部解释，再按需要加入调用方，是较容易核对的使用方式。

HTTP 与 ACP 两条路径的错误位置不同。直接模型请求失败，应看 Provider、参数和工具格式；外部代理失败，应再看进程启动、认证、工作目录与其自身权限。不能因为界面相同，就在插件配置里寻找外部代理管理的全部设置。

diff 审查仍是最终控制点。接受建议前检查是否误改选区之外内容，接受后运行相关检查。只读调查子任务能帮助找线索，却不能代表任意可写团队。升级插件、语言环境或后端时，应先重做一个小修改，确认自己依赖的日常操作仍然可靠。''',
'B18': '''一个轻量转换不一定需要完整代理。例如改写注释或转换选中代码，直接模型请求可能已足够；需要读多个文件、运行检查并纠错时，再使用完整工具循环或外部 ACP 代理。按工作尺度选择，能减少等待和不必要的配置。

自定义工作流应写清输入和输出，特别是结果应用到哪个缓冲区、是否允许跨文件修改、错误时怎样结束。一个漂亮提示模板如果没有这些约定，重复运行也可能产生不同副作用。先固定少量高频工作流，比把社区配置全部装进来更容易维护。

排错可以按编辑器、适配器、外部代理与模型四层进行。先确认选区和项目路径，再确认连接与认证，最后看工具与模型结果。只在 HTTP 路线正常，不能证明 ACP 路线也已配置好；反过来，外部代理本身可运行，也不能保证编辑器传递了正确上下文。''',
'B19': '''组织治理不是在模型列表里选一个私有端点就结束。还要确认哪些文件进入上下文、工具以谁的身份运行、日志保存在哪里以及用户能否改变设置。模型推理和工具执行可能经过不同数据路径，应按实际部署分别核对。

试点项目应覆盖组织真正困难的情形：大仓库、严格权限、内部依赖和既有检查流程。只在公开小仓库成功，无法代表内部项目同样可用。工具被拒绝时要看说明是否清楚，模型不可选时要看是否透明提示，避免静默行为让团队误判结果。

管理员更改模型或策略后，实际任务表现也可能变化。保留几项代表性基线，重点检查受到改变的读取、执行和代码应用路径。企业选型的价值不只在平均生成质量，还在行为是否可解释、可管理，以及日常支持团队能否定位问题。''',
'B20': '''独立云环境有助于减少本机隐含状态，也会暴露准备不足。测试所需数据、系统依赖或内部网络如果没有说明，任务可能在修改前就失败。将环境启动作为单独验收，才能公平评价代理实现代码的能力。

Source、Session 与 Activity 分别表达任务材料、一次运行和运行中的事件。调用方应跟踪状态与产物，不把收到某条消息等同于全部完成。通过 CLI 管理任务，也仍是与服务交互，不能因此期待本机工具、未提交文件和环境变量自动参与执行。

异步任务适合边界清楚的维护，但需求经常变化时交接成本会增加。先处理一个独立 issue，检查分支、diff、测试和已知限制；任务通知只是提醒检查成果。若计划扩大到批量维护，还应统计审查工作量，因为大量可运行却需要重写的补丁不会真正节省团队时间。''',
'A01': '''项目规则、Skill、Hook 和权限要分别理解。规则告诉模型长期遵循什么；Skill 告诉它某类工作通常怎样做；Hook 在具体事件发生时运行确定程序；权限决定工具请求是否允许。比如“修改后检查格式”可以写成规则，也可以通过 Hook 执行检查，但两者强制程度不同。规则被摘要遗漏与 Hook 配置错误，也属于不同故障。

长任务看似忘记要求时，先核对要求放在哪里、当前会话是否读取、是否在压缩后仍有足够依据。对重要约束，可以要求重新读项目文件；重复粘贴全部历史则可能把成本与噪声一起抬高。把调查交给子代理时，给它一个需要证据回答的问题，比只给“研究专家”头衔更有效；主代理需要文件位置、结论和未解决问题。

命令没有执行时，应先看是模型没有提出调用、审批没通过、沙箱拒绝访问，还是程序自身报错。每种情况的处理不同：补提示、调整合理授权、修正环境或修代码，不能一概换更强模型。正式比较时固定项目状态与工具，原生模型的优势才不会和更宽的权限、更齐的环境混在一起。''',
'A02': '''事件接口的价值在于让客户端区分“正在请求模型”“工具已开始”“需要审批”和“回合结束”。如果只把它们拼成一段文字，重新连接时就容易误判操作是否做过。开发自有客户端时，应该按协议管理任务状态；普通用户则应查看实际命令和文件，确认成功提示对应真实结果。

并行任务有三种不同的分离：子代理分开上下文，工作树分开代码目录，沙箱限制系统资源。可以只拥有其中一种，也可以组合。两个工作树仍可能使用同一个数据库或相同端口；两个子代理在共享目录里也可能争改同一文件。因此，分工应写明文件与外部资源责任，集成测试仍由整体任务承担。

恢复时至少核对四件事：当前任务目标、实际分支与文件、上次验证结果、仍在运行或已结束的外部操作。摘要里写“服务已启动”，不表示进程重启后服务还在。让代理先核对环境再继续，通常比直接说“接着做”更稳。模型升级适合解决推理瓶颈，不能替代这些状态检查。''',
'A03': '''调度层把模型意图转换成有状态的工具操作。一次读取、一次写入和一条长时间运行的命令，对取消、审批与结果保存的要求并不相同。排错时可以沿三段看：客户端有没有发出正确请求，调度器怎样决定，工具实际返回什么。这样比仅凭最后一段回答判断原因更清楚。

联网资料与项目事实也要分开。模型检索到的接口文档可能属于另一个版本，仓库却锁定旧依赖；这时不能直接照搬搜索答案。让代理检查依赖版本、请求格式与真实错误，再把外部资料接到本地实现，才是检索辅助开发的完整链条。

更换认证渠道后，模型名称相似也可能有不同额度、组织策略或服务能力。一次图片任务失败，应先检查图片是否经当前入口发送，再确认模型与渠道；一次工具失败则应检查返回信息。把这些差异记录下来，才能区分产品配置问题、工具问题和模型理解能力，而不是把所有失败都称为模型变弱。''',
'A04': '''多协议配置至少包含服务地址、认证、模型 ID 与请求格式。地址正确而协议错误时，普通文本可能勉强可用，工具、多模态或推理参数却失败；模型 ID 是别名时，还可能在不改配置的情况下切到新版本。完成一次聊天连接后，应继续测试工具往返和恢复，而不是立即迁移全部工作。

子任务创建既涉及上下文，也涉及工作目录和父任务的生命周期。若当前沙箱限制了可访问目录，独立工作树可能无法运行；若取消只结束父任务，子任务还在改文件，就会留下状态问题。源码中对此做约束的意义，是承认这些能力需要共同设计，不能分别打开几个开关就假定全部兼容。

本地模型方案还需核对推理服务的真实窗口、输出限制与工具模板。硬件不足导致截断、服务模板改变调用格式，与模型自身不会解决问题是不同情况。先用短任务排除部署问题，再增加上下文和任务复杂度；这样得到的成本与质量判断才有意义。''',
'A05': '''上下文压缩首先需要保留一段合法的对话。工具调用和工具结果必须能对应，压缩边界不能随意切在两者中间。之后才是摘要质量：目标、约束、已改文件、失败方案和下一步应能支持继续工作。只留下漂亮的项目概述，往往不足以恢复真正的工程进度。

长窗口减少了部分切片需求，却不会自动告诉模型哪些资料最关键。迁移文档、旧接口和最新代码同时存在时，需要说明时效与优先级，否则模型可能把互相矛盾的版本混用。每阶段交付一个小而可验证的成果，有助于及时发现理解偏差，也能让恢复材料更准确。

ACP 连接失败时，应分别检查客户端配置、Kimi 进程启动和模型认证。客户端能显示历史，不代表当前模型账户可继续请求；命令能在终端运行，也不代表编辑器传入了同一个目录。把入口与环境分开核对，有助于避免把连接问题误认为长任务能力不足。''',
'A06': '''模型特定工具层意味着“同一份提示＋同一个模型”也不一定是同一次实验：工具名称、参数、补丁方式和调用组织改变后，模型看到的任务接口已经不同。比较 microkernel 路线时，应记录实际启用了哪些工具，不能只记录界面上的型号。

脚本可以把独立查询合并处理，减少模型与工具之间的来回。但脚本编排一旦包含依赖关系、错误分支和有副作用的操作，仍需明确何时停止、哪些结果已经生效。脚本引擎限制只能保证其直接可用能力；调用宿主命令之后的资源边界，需要另外看权限和执行环境。

检查点的价值在恢复时最容易检验：故意在一个阶段完成后停止，再让任务说明实际文件、已通过检查及下一步。若检查点与磁盘不一致，应先核对事实再继续，不能让模型依据旧记录覆盖新改动。持久记忆、当前任务检查点与自动摘要应各有职责，避免同一条错误在多个位置被反复保存。''',
'A07': '''插件边界越开放，装配本身越需要可观察。某个 Profile 里虽然使用同一个模型，循环、工具、存储或子代理后端却可能不同。排查问题时先保存最终配置，确认实际加载了哪个服务，再沿调用路径找错误；只对照一个配置文件可能忽略叠加覆盖后的结果。

事件日志还需要区分意图与事实：记录“请求执行”不等于执行成功，记录“执行成功”也不等于业务效果正确。恢复界面可以重放事件，恢复任务则还要检查外部状态。对于不能安全重复的动作，必须先确认上一次是否已发生，而不是把重放历史当作重新执行工具。

有界并行和独占屏障降低部分执行冲突，但任务语义仍要表达依赖。读取旧文件、改写文件、再读取新结果不能任意重排；两个独立查询才更适合一起执行。开发自己的循环时，除完成路径外还应设计错误、取消与恢复，并用相同任务检验每次替换是否改变了这些行为。''',
'A08': '''开放 Provider 解决的是接入选择，不会消除模型在工具描述、推理状态与输出格式上的差异。发现模型开始重复调用或丢失工具结果时，应检查适配和历史，而不是首先增加更长提示。网关若只保留可见文本，可能使表面正常的聊天在多轮执行中失去必要信息。

会话记录、压缩摘要和文件快照分别回答不同问题：之前说过什么、下一轮需要记住什么、某时文件是什么样。恢复对话后应核对实际目录，特别是在人工已经改过文件或从其他分支带入变更时。不要让过时摘要成为覆盖现状的依据。

子会话通过派生模型与权限开展局部工作，主任务则承担汇总和验收。给子任务限定文件范围与输出形式，可以减少无关修改和主上下文噪声。启用后台或实验功能时，应把版本、开关和取消行为一并记录；“支持子代理”不足以描述这个组合是否适合长任务。''',
'A09': '''最小核心让行为来源更容易辨认，但前提是扩展数量受控。一个扩展可能注册新工具，另一个监听同一事件并改变上下文；两者各自正常，也可能在组合后产生顺序或状态问题。出现异常时从最小配置重现，逐个恢复扩展，比直接修改代理核心更容易定位。

会话树保留的是讨论与运行事件的分支。回到较早节点可以换一种思路，却不会让外部数据库、命令进程和文件自动倒退。需要比较两套实现时，应同时安排代码版本和对话分支；否则模型沿旧思路继续时，看到的可能已经是另一方案修改后的文件。

默认缺少内置权限系统，使运行边界尤其依赖启动方式。隔离方案不只是给程序套一个名字，还应检查目录挂载、网络和凭据。对研究者，这让实验条件可以明确控制；对普通用户，则意味着不能把全部产品治理责任交给默认安装。自由扩展与维护责任是一组相连的取舍。''',
'A10': '''透明交互只有在用户看得懂时才有价值。审批消息应让你知道操作对象、目的和可能影响；如果每次都无条件批准，虽然界面保留了步骤，实际上已失去大部分人工判断作用。可以让模型先解释不熟悉的命令，再根据任务缩小授权，而不是只在全开与全关之间选。

只读子代理适合回答清楚的问题，例如“这个配置有哪些覆盖入口”，交回路径与依据。它不适合承担本身需要大量写入验证的工作。CLI 团队又要管理任务板、消息与持久状态，主任务必须决定冲突怎样处理。把这两类功能混写，会使用户在错误入口期待并不存在的能力。

长任务失败时先看上下文管线与服务错误：模型输出受限、服务临时失败、压缩丢约束与真正改错代码，处理方式不同。恢复后让代理重新确认关键文件与测试结果，可以避免继续沿错误摘要工作。过程可见性应最终转化为较少的误改和返工，而不是更长的日志。''',
'A11': '''比较 Kilo 与上游结构时，应观察产品行为的接缝：同样发生上下文溢出，是否压缩重试、怎样展示错误、哪些模型信息被记录，都可能改变实际体验。账户提供的模型目录与开放核心支持的协议，也不一定是同一范围。了解这两层能帮助解释为什么共享部分代码的产品仍然不同。

BYOK 主要说明主模型请求使用自己的凭据，并不自动说明云任务、知识服务或其他辅助能力免费。一次完整试用应同时检查 Provider 账单与产品消费，记录失败重试；否则很容易拿单次模型请求价格代表整套产品成本。

迁移旧 Roo 或 Cline 配置时，不应只认熟悉的名字。逐项检查模式、项目指令、MCP、历史与权限的实际格式；先迁一条可验收流程，再迁其他内容。跨本地和云端时还要重新准备环境，因为本机能读取的文件、密钥和正在运行的服务不会自动出现在远端。''',
'A12': '''工具扩展像接入不同业务窗口，每个窗口都有自己的账户、数据范围和错误形式。一个只读数据库查询、一个文件工具和一个工单接口，即使都通过 MCP 连接，也不代表拥有相同权限和返回质量。先让每个工具独立完成小任务，再让代理组合使用，较容易定位错误。

长返回值会影响上下文：一整张大表既贵，又可能让模型错过关键字段。更合理的方案是由工具或查询限定范围，保留筛选条件和记录 ID，需要时再回查。自动摘要可以缓解容量，但不能保证全部业务细节保留；特别是数字报告，应把计算过程与原始结果另存为可核对材料。

ACP 使用还需区分方向。编辑器驱动 Goose 时，主要换的是交互入口；Goose 转用另一个代理后端时，执行语义也可能变化。费用、认证、权限和恢复都要跟着实际运行者判断。所谓统一工具生态，能够减少连接方式差异，却不会抹平这些产品责任。''',
'A13': '''队列与中途输入看似是界面细节，其实会影响任务正确性。用户补充一个约束后，应能判断它进入当前回合还是后续回合；长命令运行时，取消是否结束了对应操作也需要观察。优秀终端体验应帮助用户看清状态，而不是只让文字更好看。

大模型与小模型配置可以将辅助任务放到较经济路线，但主任务仍可能承担主要输入、工具和重试成本。比较时应查完整消费，不把界面显示的小模型名称理解成全部任务都按小模型计价。模型切换也应检查新配置是否用于实际运行，而不是仍由旧实例继续。

SSH 场景中，启动 Crush 的那台机器才是文件和进程所在处。当前终端看起来相同，工作目录、服务端口与凭据却可能完全不同。故障先分主机和目录，再分模型与工具；这样能避免把远端环境缺依赖的错误当成代理写代码能力不足。''',
'A14': '''Python 接口分层使模型请求、工具和权限可以分别观察。一次调用失败，先区分模型服务拒绝参数、动作格式无法解析、权限不允许，还是程序运行报错。只在最后一种情况里改业务代码，能够减少代理为绕过环境问题而做无关修改。

Ask、Plan 和接受编辑等模式的意义是工作范围不同，不能只凭标签判断所有操作。尤其命令工具可能同时包含读取与写入效果，最终仍需看实际策略和执行环境。给子任务配置模型与工具时，也要确认它的权限和父任务要求一致。

开放模型的优势在可部署与可调整，但服务端模板、量化和上下文设置都可能影响多轮调用。小型号更省资源，却可能花更多回合纠错；强型号可能更贵，但一次完成率更高。比较应包括完整任务、实际硬件与人工检查，而不是只按参数量或模型家族标签作结论。''',
'A15': '''环境生命周期包括准备、运行、保存和回收。任务运行时产生的文件若只留在临时工作空间，销毁环境后就可能无法交付；相反，长期不回收又会消耗资源并保留过期凭据。可靠流程应先明确哪些产物要持久保存，再让环境结束。

独立工作机能够减少开发者本机的隐含状态，却要求把依赖和服务关系写清楚。某个测试需要数据库、浏览器或外部 API，应给出可重复配置或合理替代；代理不断尝试安装依赖，未必意味着模型不够强，可能只是准备材料缺失。

Canvas 接原生 SDK 与接 ACP 外部代理是不同组合。比较时要记录前端、后端和工作空间，特别关注谁管理审批、历史与取消。批量任务还需限制并发和资源，检查失败后的清理。开放环境控制提供了空间，也把相应运维责任交给使用者。''',
'A16': '''配置分层的实际作用在冲突时最清楚。项目希望放宽某项工具，而组织策略仍限制它，最终行为应按有效策略理解；单独读项目文件会产生错误预期。inspect 类能力帮助将“写了什么配置”转成“真正生效什么”，适合在复杂部署中作为排错起点。

脚本入口需要调用者处理状态，而非只接收一段自然语言。任务可能因为权限、认证、环境或预算未完成，程序应区分这些情况；看到模型生成了文字，不代表它修改了文件。需要结构化结果时应采用文档所述格式，并在一个故意失败的任务上检验调用方能否识别。

公开源码的同步节奏与发行版也应一起记录。研究某个提交里的工具或沙箱实现，可以证明那个版本存在相应路径，但不能直接推断所有已安装二进制已经包含。遇到功能差异先核对版本与构建，再比较模型；这样不会把发布差异误判为原生模型能力差异。''',
'A17': '''这些机制应分别验收：权限决定能否执行，目标状态决定是否继续，调度决定何时触发，发布插件决定产物怎样交付。某一项可用不能替另一项作保证；特别是可恢复的任务记录与仍在运行的进程，需要分别检查。''',
}
