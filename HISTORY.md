# 报告与仓库沿革

本项目在前三轮报告工作完成后才开始使用 Git。以下三个报告版本依据本任务保存的文件编辑记录、采集索引和旧稿恢复，随后按原顺序导入；Git 的提交日期使用实际导入时间，未伪造为过去已经存在的提交。原始会话及工具日志不上传到仓库。

| Git 标签 | 原工作阶段 | 主要内容 |
|---|---|---|
| `report-v1` | 首次撰写，2026-09-22 | 建立中文术语、全景清单、45 个核心工具档案、架构比较、40 个细分场景、模型与成本、试用及参考资料；阅读版含 5 幅图 |
| `report-v2-stepfun` | 第一次修改，2026-09-23 | 增加 Step Code 与 Step-5-Preview，核心工具增至 46 个、公开仓库记录增至 42 个；当时包含后来被要求撤除的专项章节与横幅 |
| `report-v3-unified` | 第二次修改，2026-09-23 | 扩充 A、B、C、D 与维护观察，共 55 份统一七段式档案、60 幅图、155 张表；将 Step 融入通用比较，移除特殊导航与增补提示 |

随后单独提交本次仓库管理工作：忽略缓存、制定 `AGENTS.md`、补齐可复现构建依赖与来源哈希，并建立不依赖第三方下载缓存的常规检查。

## 如何确认恢复结果

- 首次撰写的文件按原编辑记录恢复，图表、索引和 HTML 使用对应阶段的生成脚本重建。没有回放网络采集、浏览器操作、安装器或产品运行测试。
- 第一次修改按原编辑记录恢复，排除了当时被工具拒绝而未生效的补丁。恢复后的 README 与 12 份报告 Markdown 均与 `research/editorial-baseline/` 保存的旧稿逐字一致。
- 第一次修改的 HTML SHA-256 与当时交付摘要保存的哈希相同：`d1733407cc5b0bda475737f8dd27c2c180c8e579411cd5edf3be56f361d60d57`。
- 第二次修改直接使用开始本次 Git 管理前的现有文件快照。HTML SHA-256 为 `80827c6054f9655b68be6f8d8b29eb356cad341cfd5a6fa0320a5da2a61ca7d1`。
- 首版重建 HTML SHA-256 为 `ad96314a0ec221f012adbddf1ee96c69416f44685d25bdc77a3d1a9f7702f66a`；没有保存的原始首版 HTML 哈希可供独立比对，不把重建结果声称为已经完成该项比对。

三个历史导入阶段统一应用当前 `.gitignore`，未提交约 854 MB 的第三方源码缓存，也未提交网页缓存、机器依赖、会话原文、阅读摘录或截图缓存。因此它们是项目文稿与生成来源的历史，不是当时整个工作目录的逐字节备份。部分生成的 QA 摘要是恢复时重新执行的静态检查；保留的旧视觉记录只适用于其对应版本。

`research/editorial-baseline/` 虽含旧版文字，仍被当前编排脚本读取，因此继续保留。读者应以 `report/` 和根目录 HTML 为当前版本。

## 查阅变化

```bash
git log --oneline --decorate --reverse
git diff --stat report-v1 report-v2-stepfun
git diff --stat report-v2-stepfun report-v3-unified
git show report-v1:README.md
```

旧版本标签用于追溯内容；从干净克隆构建时请使用当前主分支。早期脚本依赖当时的本地运行环境，这一限制在本次仓库管理提交中修正。
